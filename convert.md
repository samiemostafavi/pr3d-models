# Convert PR3D models to Scipy models

Install dependencies from `pr3d_requirements.txt`:
```
pip install -Ur pr3d_requirements.txt
```

Configure the script with correct folder and file names, then run:
```
python convert.py
```